// import { getSearchUrl } from '@/shared/api/config';
// import { axiosClassic } from '@/shared/api/interceptors';

// TODO: доделать
export const searchProducts = async (data: any) => {
  //   const response = await axiosClassic.post(getSearchUrl(), data);
  //   return response;
};
